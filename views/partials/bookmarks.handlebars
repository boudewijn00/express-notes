{{#each notes}}
    <div class="box">
    <p class="title is-6">{{@key}}</p>
    <ul>
    {{#each this}}
        <li class="block">
            <a target="_blank" class="is-underlined" href={{this.body}}>{{this.title}}</a>
        </li>
    {{/each}}
    </ul>
    </div>
{{/each}}