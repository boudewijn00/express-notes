{{#each notes}}
    <div class="box">
    <p class="title is-4">{{@key}}</p>
    <ul>
    {{#each this}}
        <li class="block">
            <a aria-label="external link" target="_blank" class="is-underlined" href={{this.body}}>{{this.title}}</a>
        </li>
    {{/each}}
    </ul>
    </div>
{{/each}}