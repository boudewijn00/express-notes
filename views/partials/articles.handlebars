{{#each notes}}
    <div class="box">
        <p class="title is-6">{{@key}}</p>
        {{#each this}}
            <div class="content">
                <h1 class="title">{{this.title}}</h1>
                {{{markdown this.body}}}
            </div>
        {{/each}}
    </div>
{{/each}}