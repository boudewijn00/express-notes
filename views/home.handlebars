<div class="columns">
    <div class="column is-half">
        <div class="box">
            <div class="content">
                <h1 class="title">{{ note.title }}</h1>
                <p>{{ note.body }}</p>
            </div>
        </div>
    </div>
</div>